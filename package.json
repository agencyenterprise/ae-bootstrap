{
  "name": "ae-bootstrap",
  "version": "1.0.0",
  "description": "Add standard files for AE Projects",
  "repository": {
    "type": "git",
    "url": "git+http://tmp/"
  },
  "main": "index.js",
  "bin": {
    "ae-bootstrap": "src/index.js"
  },
  "scripts": {
    "clear": "rimraf dist",
    "build": "tsc && npm run copyfiles",
    "build-watch": "tsc -w",
    "copyfiles": "npm run copyfiles-git && npm run copyfiles-tslint && npm run copyfiles-format && npm run copyfiles-circleci",
    "copyfiles-git": "copyfiles .gitignore-* .github/**/*.* ./dist",
    "copyfiles-tslint": "copyfiles tslint-*.json ./dist",
    "copyfiles-format": "copyfiles .editorconfig .prettierrc tslint.json ./dist",
    "copyfiles-circleci": "copyfiles .circleci/**/*.* ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.3.0",
    "commander": "^2.15.1"
  },
  "devDependencies": {
    "@types/colors": "^1.2.1",
    "@types/node": "^10.3.5",
    "copyfiles": "^2.0.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.10.0",
    "tslint-config-codingwise": "file:../../tslint-config-codingwise/tslint-config-codingwise-0.0.1.tgz"
  }
}
